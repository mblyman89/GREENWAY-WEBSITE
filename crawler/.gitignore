# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets & local state
.env
!.env.example
.cache/
*.log
# Cultivera cached session (holds a live JWT) — never commit, even if the
# operator points CULTIVERA_SESSION_FILE outside .cache/.
cultivera_session.json

# PyCharm (keep shared run configs in .run/, ignore per-user state)
.idea/
