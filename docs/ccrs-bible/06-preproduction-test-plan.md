# 06 — PREproduction Test Plan

Purpose: turn every unknown into a recorded PREprod result **before** the first production upload. The owner has never uploaded from this system (Part 05 D-01); PREprod is the only safe place to learn what CCRS actually accepts.

Facts the plan rests on:
- "The LCB provides all cannabis licensees, labs and integrators access to the PREproduction CCRS environment for training and testing." `[FAQ L0088]`
- "These environments are autonomous and do not share administration or reporting data." `[FAQ L0096]` — nothing done here touches the real record.
- PREprod host `precannabisreporting.lcb.wa.gov` `[API L0070]`; production `cannabisreporting.lcb.wa.gov` `[G L0048]`.
- Errors arrive by email to the uploader `[G L0051]`, `[FAQ L0102]`. There is no success email for the seven weekly files in any LCB text (only Manifest gets a PDF `[MANI L0015]`) → **absence of an error email within the wait window is the only "pass" signal**. Record the wait used.
- Groups: Strain/Area/Product → ≥10 min → Inventory → ≥10 min → Adjustment/Transfer/Sale `[G L0530]`, `[G]` p.4.

## A. Recording rule (binding)

Every test row below is executed by a human at the PREprod portal with files generated by this app (hub, environment switch set to PREPRODUCTION — Part 08 §F). For each test record in `ccrs_upload_events` (slice S-06) or, until that ships, in `docs/CCRS_COMPLIANCE_AUDIT.md` under a heading `PREprod results`:
`test id | date/time Pacific | file name(s) | wait minutes | error email? (Y/N) | verbatim error lines | conclusion | Part 12 item closed`.

Do not summarize error emails. Paste them whole.

## B. Access tests

| ID | Test | Pass criterion | Closes |
|---|---|---|---|
| T-00 | Owner asks examiner (or enforcement officer) to confirm Greenway's license is provisioned in PREprod and which login (SAW today, WA.gov later) it uses | written reply on file | U-01 |
| T-01 | Log in at `https://precannabisreporting.lcb.wa.gov` following the login guide `[LOGIN]` (do NOT log into SAW directly `[SAW L0004-L0005]`) | reaches CCRS PREprod landing showing the license | U-01 |
| T-02 | Account → Licensee shows Greenway license; note whether Cultivera appears under Approved Integrators in PREprod (expected: no — environments are autonomous `[FAQ L0096]`) | screenshot filed | info |

## C. File-format tests (Group 1 first; each is one upload of one file unless stated)

| ID | Test | Files | Pass criterion | Closes |
|---|---|---|---|---|
| T-10 | Baseline Strain file exactly as the app emits today (unpadded header rows, CRLF, UTC stamp) | `strain_<lic>_<stamp>.csv` with 2 rows | no error email in 30 min | U-03 (partially), baseline |
| T-11 | Same Strain content with header rows padded like the template (`SubmittedBy,<v>,,,,,` etc.) | one file | no error email | U-03 — if T-10 errors and T-11 passes → pad; if both pass → no padding needed (prefer template shape anyway) |
| T-12 | Filename prefix case: upload `Strain_<lic>_<stamp>.csv` (capital S) | one file | error or not | U-02 |
| T-13 | Filename stamp in PST vs UTC: two Strain files stamped 1 s apart, one UTC one Pacific, at a time where dates differ (after 17:00 Pacific) | two files | note which (if either) is refused | N-05 / U-02 |
| T-14 | Strain with name `Unknown` | one file | expect error "Strain name is invalid, cannot be Unknown, THC, or Other" `[G L0358]` — record exact text for triage rules | E11 rule text |
| T-15 | Duplicate Strain (re-upload T-10) | one file | expect either silence or "Duplicate Strain" `[G L0359]`; FAQ/guide say no action needed | N-08 |
| T-16 | Area file with `Sales Floor`/FALSE only | one file | pass | baseline |
| T-17 | Area file with `Quarantine`/TRUE (today's emitter) | one file | pass/fail; regardless, **ask the examiner** whether a retailer hold area should be TRUE given `[G L0258-L0259]` ("Examples of areas with a 'quarantine' designation: waste/ destruction inventory") vs `[G L0298-L0299]` ("There are no quarantine requirements for cannabis products and must have an entry as False") | U-04 / N-01 |
| T-18 | Product file: one Usable Cannabis row with `UnitWeightGrams=0` | one file | expect error `[G L0434]` — capture text | E9 rule text |
| T-19 | Product file: one Usable Cannabis row with empty Description | one file | expect error — capture text | E10 rule text |
| T-20 | Product file: full weekly product set as emitted | one file | no error | baseline |
| T-21 | Hyphenated ExternalIdentifier (e.g. `GW-TEST-1`) in Product | one file | pass/fail | U-06 |

Wait ≥10 minutes after Group 1 before Group 2 `[G L0530]`.

## D. Inventory tests (Group 2)

| ID | Test | Pass criterion | Closes |
|---|---|---|---|
| T-30 | Inventory file as emitted for a small set of lots, all with TotalCost > 0 | no error | baseline |
| T-31 | One row `TotalCost=0.00` | expect "TotalCost cannot equal 0" `[G L0614]` | E7 rule text |
| T-32 | One row `QuantityOnHand > InitialQuantity` | expect "QuanityOnHand is greater than InitialQuantity" `[G L0597]` (note LCB's spelling) | E8 rule text |
| T-33 | Re-upload T-30 unchanged (Insert again) | pass or "duplicate"? — decides N-03 strategy | U-05 |
| T-34 | Same lots as `Update` with `UpdatedBy/UpdatedDate` set and QuantityOnHand changed | pass | N-03 |
| T-35 | `Update` for an id never inserted | expect error (FAQ: "The record doesn't exist, so an error message would be received." `[FAQ L0053]`) — capture text | E16 rule |
| T-36 | Row whose Strain is not in PREprod | expect "Strain Name reported is not linked to the license number" `[G L0557]` | E16 rule |
| T-37 | Row whose Product name differs by case/space from Product file | expect "Invalid Product" `[G L0579]` | E16 rule; confirms exact-match rule |
| T-38 | Row with `IsMedical=TRUE` | expect accepted syntactically (no medical validation in guide errors list) — informational only; **never** in prod until Part 10 | info |

Wait ≥10 minutes.

## E. Group 3 tests

| ID | Test | Pass criterion | Closes |
|---|---|---|---|
| T-40 | Sale file, 3 retail lines, math per FAQ example (`QTY=3, $5.00, Discount $3.00, SalesTax $1.20, OtherTax $4.44`) `[FAQ L0155-L0160]` | no error | E12 baseline |
| T-41 | Sale line with `CannabisExciseTax` off by $0.01 | pass/fail — tells us the tolerance | E12 tolerance |
| T-42 | Sale line with excise `0.00`, SaleType `RecreationalRetail` | expect "Only Medical Sales can be 0" `[G L1378]` | E16 rule |
| T-43 | Sale for a lot in the TRUE area | expect "Sold item cannot be in Quarantine" `[G L1305]` | W4 → error, N-01 |
| T-44 | Two rows same `SaleExternalIdentifier`, different `SaleDate` | expect "Duplicate Sale for Licensee" `[G L1390]` | N-09 rule |
| T-45 | Re-upload T-40 unchanged | expect duplicate error — confirms Sale is one-shot Insert | U-07 |
| T-46 | Sale `Delete` for one line of T-40 (the FAQ return procedure `[FAQ L0039]`) then InventoryAdjustment `Other` with detail | no error | E13, U-09 |
| T-47 | Sale `Update` for a partial return (today's `mapSaleCorrectionRow` path) | pass/fail | U-09 — if fails, corrections must be Delete+Insert |
| T-48 | InventoryAdjustment reason `Other` with empty detail | expect "Inventory AdjustmentDetail missing" `[G L1111]` | E13 |
| T-49 | InventoryAdjustment `Destruction`, `Reconciliation` rows with positive Quantity | pass | baseline |
| T-50 | InventoryTransfer row: From = a vendor license, To = Greenway, From id = an id that does NOT exist in PREprod | expect "Invalid FromInventoryExternalIdentifier" `[G L1191]` — confirms CCRS validates against the vendor's filed inventory | U-08 |
| T-51 | InventoryTransfer with `From == To` license | expect "ToLicense cannot be the same license number as FromLicense" `[G L1205]` | E16 rule |
| T-52 | InventoryTransfer re-upload | expect "Duplicate InventoryTransfer for Licensee" `[G L1190]` | E16 rule (benign per triage) |
| T-53 | Multi-file select: upload all Group 3 files in one action `[G L0163-L0165]` | processes | UI knowledge |
| T-54 | `NumberRecords` one less than rows | expect fail-to-process `[G L0204-L0205]` — capture text | E16 rule |

## F. Cutover rehearsal (Part 07) — run in PREprod once S-05 ships

| ID | Test | Pass criterion |
|---|---|---|
| T-60 | Upload the FULL Group 1–3 batch for one real completed week (data as of the parallel run) with the environment switch on PREPRODUCTION | zero error emails after 30 min for each group |
| T-61 | Repeat T-60 the next week with Inventory as `Update` where N-03 says so | zero errors |
| T-62 | Paste any error email into the hub (S-06), confirm triage maps every line to a rule and a fix-link | 100 % of lines classified; none "unknown" |

## G. Questions for the examiner (send as one email; log answers in Part 12)

1. Is Greenway (license number from `license_settings`) provisioned in PREproduction, and which login does it use today? (U-01)
2. Must the three header rows be comma-padded to the column count as in the templates, or is `SubmittedBy,<value>` acceptable? (U-03)
3. Is the file-name prefix case-sensitive (`sale_` vs `Sale_`)? (U-02)
4. For a retailer with no imported CBD, should any Area ever have `IsQuarantine=TRUE`? Page 9 lists "waste/destruction inventory" as a quarantine example, page 9 also says cannabis "must have an entry as False". (U-04)
5. For inventory already reported weekly by our integrator (Cultivera) under our license using the product barcode as InventoryExternalIdentifier: when we begin uploading ourselves, should we (a) continue `Update` on those same identifiers, or (b) `Insert` new identifiers and file InventoryTransfer rows old→new? (U-08 / Part 07)
6. Does re-`Insert`ing an unchanged Inventory row weekly produce an error or a silent no-op? (U-05)
7. For a partial customer return, is `Update` of the Sale row acceptable, or must it be `Delete` + re-`Insert`? (U-09)
8. Will the Upload page or file formats change with the October 2026 WA.gov transition? (U-10)
9. Is there any success/receipt notification for the seven weekly files, or is silence the only confirmation? (U-11)
10. May an Inventory `Update` change the Product a lot references (our Product names differ from the integrator's)? `[G L0579-L0583]` (U-12)
11. How long must a licensee retain its uploaded CSV files and the CCRS error emails? (U-13)
12. For an intake lot with no purchase cost on file that is a trade sample, is `TotalCost = 0.01` acceptable in Inventory, in line with the $0.01 sample guidance `[FAQ L0035]` and "TotalCost cannot equal 0" `[G L0614]`? (U-16)

Send with the CSV attached when asking about a specific error, as the FAQ instructs `[FAQ L0030]`.
